{
  "name": "@acprompt/agent-bus-client",
  "version": "1.0.0",
  "description": "TypeScript SDK for ACPrompt agent communication bus — auto-reconnect WebSocket, pub/sub, request-reply, type-safe envelopes",
  "main": "dist/client.js",
  "types": "dist/client.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["agent", "message-bus", "websocket", "pubsub", "request-reply", "acprompt"],
  "license": "MIT",
  "peerDependencies": {
    "ws": ">=7"
  },
  "peerDependenciesMeta": {
    "ws": { "optional": true }
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/ws": "^8.5.0"
  }
}